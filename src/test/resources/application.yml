spring:
  profiles:
    active: test
  application:
    name: "spring-mvc-project"
  liquibase:
    enabled: true
    change-log: db/db.changelog-master.yaml
  jpa:
    hibernate:
      ddl-auto: none